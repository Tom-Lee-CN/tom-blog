---
description: vuepress-blog 个人博客项目 AI 编程行为最高规范 + 项目核心技术规范（v1.0.0 三层优先级体系）
alwaysApply: true
enabled: true
updatedAt: 2026-08-19T00:00:00.000Z
provider: 
---

# vuepress-blog - AI 编程必须遵守的规则

> **版本**：v1.0.0
> **适用项目**：vuepress-blog（基于 VuePress 2 + Vue 3 + Vite 的纯前端个人博客，技术 + 生活混合内容，部署到 GitHub Pages）
> **文档性质**：AI 编程行为最高规范，所有规则均为强制遵守

## 第〇章 总则与优先级体系

### 0.1 优先级铁律

| 优先级 | 铁律内容 | 裁决规则 |
|---|---|---|
| 第一优先级 | 内容正确性——Markdown 渲染与 frontmatter 必须准确 | 内容正确性与任何其他需求冲突时，一律优先内容正确性 |
| 第二优先级 | 构建稳定性——必须能成功构建并部署到 GitHub Pages | 构建稳定性与功能、开发效率冲突时，一律优先构建稳定性 |
| 第三优先级 | 功能完整性、可维护性、可扩展性、通用性 | 在保证上述前提下追求 |

### 0.2 三层优先级体系

| 层级 | 标识 | 违反严重度 | 修复优先级 |
|---|---|---|---|
| T1 铁律 | `T1-XX` | 致命 BUG | 立即修复，阻断一切后续工作 |
| T2 强制 | `T2-XX` | 严重 BUG | 当前任务完成前修复 |
| T3 推荐 | `T3-XX` | 一般问题 | 尽快修复，可记录到 TODO |

### 0.3 豁免机制

所有规则均不可由 AI 自主豁免。豁免必须由用户明确批准。AI 遇到规则冲突时必须调用提问工具请求裁决。

---

## 第一章 T1 铁律层（致命级）

**T1-01【内容正确性最高优先级】** 所有 Markdown 文档、frontmatter 字段、链接、图片引用必须准确无误。frontmatter 的 title/date/categories/tags/description 等字段必须格式正确，不得出现渲染错误、链接失效、图片缺失。

**T1-02【构建稳定性第二优先级】** 任何修改都必须保证 `pnpm docs:build` 能成功构建并部署。不得引入导致构建失败、部署失败或页面 404 的改动。构建稳定性与功能、开发效率冲突时一律优先构建稳定性。

**T1-03【功能完整性第三优先级】** 不得以第三优先级为由牺牲内容正确性或构建稳定性。

**T1-04【企业生产级别】** 必须在清楚项目现有代码之后再开始编码，不得凭记忆或压缩前上下文直接编写。

**T1-05【先看最新代码】** 每次接收到用户提问时，必须先 Read 相关最新代码，不能使用旧代码或凭压缩前记忆编码。

**T1-06【设计先行文档优先】** 重大重构、新系统、新功能必须先撰写设计文档，文档通过人工审查后才能编码。

**T1-07【渐进式替换】** 需要替换或移除代码时必须使用渐进式方式：先提问工具说明计划得到用户允许 → 新增等价代码并验证 → 再移除旧代码 → 移除后再次验证。严禁未经用户允许直接删除。

**T1-08【不误删代码与中文注释】** 绝不能误删任何有效代码，也绝不能误删任何中文注释（可以更新注释内容）。

**T1-09【数据驱动设计】** 对象严禁使用中文 key，严禁硬编码数据，所有数据应存在配置文件中，站点配置、导航、frontmatter 字段等必须数据驱动。

**T1-10【注释规范】** 所有 JS 代码必须有轻量中文注释。注释优先单行（`//`），多行不超过 3 行。严禁使用 JSDoc（禁止 @param/@returns/@type 等标签），现有 JSDoc 在日常编码时顺手移除（渐进式）。注释说明"为什么"而非"是什么"。文件头说明不超过 3 行。

**T1-11【提问沟通】** 如果有任何疑问必须调用提问工具询问用户，直到十分清楚需求才能开始。不得擅自假设用户意图。

**T1-12【不擅自结束对话】** 任务完成后绝不能擅自结束对话，必须调用提问工具与用户沟通。每个阶段完成后必须自审 + 调用提问工具。

**T1-13【上下文压缩后先沟通】** 每次 AI 上下文被压缩后继续任务时，必须先调用提问工具与用户沟通，确认最新任务需求。

**T1-14【禁用 Agent】** 严禁使用 Search Agent、Sub Coding Agent、Plan Agent。不得调用任何子 Agent。文件搜索用 Glob / Grep / Read 工具。

**T1-15【代码完成后必审查】** 完成每一个任务后都必须再检查一遍代码并排查 BUG 和优化代码。自审维度：逻辑正确性、构建稳定性、内容正确性、规则合规、BUG 排查、代码优化。

**T1-16【禁止回归性 BUG】** 修改或新增代码时绝不能引入回归性 BUG。回归定义：原本正常的功能因本次改动而退化或行为不一致（如时序竞态导致的偶发失效）。编码时必须做到：
- 修改前先理解完整调用链与边界条件，不得只改"表象"而忽视"根因"
- 新增判断条件必须覆盖全部运行分支（含快速/慢速加载等不确定性场景），确保结果稳定可复现
- 完成后自审必须专项检查"本次改动是否破坏了原有行为"，并对比改动前后行为差异

---

## 第二章 T2 强制层 - 工程规范（严重级）

### 2.1 代码质量

**T2-01【最佳实践】** 尽可能做到最佳实践、最佳性能、最佳质量、最佳扩展性和最佳通用性。

**T2-02【最新语法与 VuePress 2】** 所有代码采用最新语法和最新 API。必须使用 VuePress 2（Vue 3 + Vite）规范，不得使用旧的 VuePress 1.x API 或 Vue 2 语法。

**T2-03【纯前端无后端】** 本项目为纯前端博客，无后端服务。不得引入需要后端支撑的功能（如服务端渲染逻辑、数据库、服务器 API）。所有数据来自 Markdown 文件与静态配置。

**T2-04【多规模内容支持】** 博客既要能承载少量文章的小型使用，也要能承载大量文章（数百篇）的中型规模，分类/标签/归档页在大量文章下仍须正常渲染。

### 2.2 模块化与文件结构

**T2-05【职责分离】** 充分采用职责或功能进行模块化分离，避免单个文件过大。一个文件只承担一个明确职责。

**T2-06【JS 文件行数限制】** JS 文件最好限制在 400 行内。特殊情况可超过 500 行，但不能超过 600 行。超过 600 行必须拆分。

**T2-07【Vue 文件行数限制】** Vue 文件最好限制在 800 行内。特殊情况可超过 900 行，但不能超过 1000 行。超过 1000 行必须拆分。

**T2-08【Vue 代码分离】** Vue 的 JS 代码进行模块化分离，UI 控制代码和专属逻辑代码放到 Vue 同级目录下的 composables 文件夹中。

### 2.3 Vue 3 规范

**T2-09【响应式数据管理】** 组件状态用 ref/reactive，跨组件共享用 Pinia store。computed 用于派生状态。watch/watchEffect 必须在组件销毁时停止。不得在 watch 回调中直接修改被监听状态。大列表用 v-memo 或虚拟滚动。

**T2-10【Store 设计规范】** Store 按功能域划分。State 只存数据，Actions 存逻辑，Getters 存派生状态。异步操作在 Actions 中。Store 销毁时必须清理所有副作用。

**T2-11【组件设计规范】** 组件 props 必须声明类型和默认值。组件 emits 必须声明事件名。组件不得直接访问全局单例。组件销毁时必须清理事件监听、定时器、DOM 监听。

### 2.4 内容与 Frontmatter 规范

**T2-12【Frontmatter 规范】** 每篇文章必须包含完整且正确的 frontmatter 字段：title、date、categories、tags、description。可选字段：author、cover、pinned、draft。字段值必须合法，不得使用中文 key。

**T2-13【文件命名规范】** 文章文件必须使用 `YYYY-MM-DD-slug.md` 格式命名。日期必须与 frontmatter 的 date 字段一致。slug 使用小写字母、数字、连字符。

**T2-14【链接规范】** 站内链接必须使用相对路径或基于 `base` 的完整路径，不得使用绝对路径导致部署后 404。图片引用必须确保文件存在且路径正确。

### 2.5 样式与响应式

**T2-15【暗黑模式规范】** 暗黑模式必须使用 CSS 变量实现，严禁硬编码颜色值。颜色、背景、边框等必须通过 CSS 变量在亮/暗主题间切换。所有自定义组件必须同时适配亮暗主题。

**T2-16【响应式规范】** 必须支持移动端（<768px）、平板（768-1024px）、桌面（>1024px）三档断点。移动端单列布局 + 汉堡菜单，桌面侧边栏 + 内容区。

### 2.6 错误处理与日志

**T2-20【异常捕获】** 所有外部调用、异步操作必须有 try/catch。Promise rejection 必须有 catch。

**T2-21【错误传播】** 错误必须传播，不得静默吞掉。捕获后若无处理逻辑，必须重新抛出或记录到日志。

**T2-23【禁用 console】** 生产环境禁用 console.log/warn/error，统一用日志系统。开发环境可临时使用但提交前必须移除。

### 2.7 安全规范

**T2-25【输入校验与 CSP】** 用户输入必须校验。CSP 策略配置，限制资源加载来源。

### 2.8 依赖管理

**T2-28【GC 风险评估】** 第三方库选型必须评估包体积与性能风险。

**T2-29【版本锁定】** 依赖版本必须锁定，不得使用 latest 或范围版本。

**T2-30【新增依赖确认】** 新增依赖必须经用户确认。AI 不得擅自安装新依赖。

### 2.9 文档与任务流程

**T2-31【文档先行审查】** 重大重构、新系统必须先写设计文档并通过人工审查才能编码。

**T2-32【任务总结】** 任务完成后必须给用户总结：做了哪些功能、哪些代码、哪些注释，具体到文件和行数。

**T2-33【阶段切换确认】** 阶段切换必须调用提问工具获得用户允许。

**T2-34【不运行 dev/build】** 不运行 `pnpm docs:dev` 和 `pnpm docs:build`，因为构建输出不在终端直观验证。验证通过代码审查和逻辑推演。

**T2-35【不运行 eslint】** 不运行 eslint，本项目未使用 eslint。

---

## 第三章 T3 推荐层 - 最佳实践（一般级）

**T3-06【分层配置】** 分层配置：DEFAULT/USER/RUNTIME/DEBUG，四层优先级递增。

**T3-07【命名规范】** 类名 PascalCase，函数/变量 camelCase，常量 UPPER_SNAKE_CASE，私有成员下划线前缀。

**T3-08【代码组织】** 一个文件一个主类/主函数。import 顺序：第三方库 → 公共模块 → 业务模块。避免循环依赖。
